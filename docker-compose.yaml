# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: '01tranduc'
      POSTGRES_PASSWORD: '1234567Duc'
      POSTGRES_DB: 'DB'
    ports: 
      - 5433:5432
    volumes:
        - ./data:/var/lib/postgresql/data